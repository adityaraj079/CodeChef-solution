#include<bits/stdc++.h>
typedef long long ll;
using namespace std;

int main()
{
    int T;
    cin>>T;
    int N,M,K=1;
    int test=pow(2,N);
    while(T--)
    {
        cin>>N>>M>>K;
        if (M <= min(N*(N-1)/2,1000) && K <= min(test,200))
        {
            
        }
    }
}